// router tools index file
export { routerModel } from './routerModel.js';
export { routeToToolSet } from './routeToToolSet.js';

// export router tools array (currently empty as router is a model, not a tool)
export const routerTools = []; // router doesn't have traditional tools

export const routerToolsNode = null; // router doesn't need a tool node 